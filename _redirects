# Static demo page — must come BEFORE the wildcard rule
/gear/example  /gear/example/index.html  200

# Live gear viewer for any /gear/:slug URL.
# Rewrite to the directory "/gear/" (NOT /gear/index.html) — Cloudflare
# 307-redirects */index.html to the directory, which would strip the slug.
/gear/:slug  /gear/  200
